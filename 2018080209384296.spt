var appId = $("#game").val();
            var channelId = $("#channelHideId").val();
            var subChannel = $(this).val();
            var lpId = $("#pageId").val();
            $.get(ctxPath + "/channel/generatingLink",{channelId:channelId,appId:appId,subChannel:subChannel,lpId:lpId}).done(function(res){
                if (res.code == ghw.constant.resp_status.success) {
                    $("#link").val(res.data);
                }
            });